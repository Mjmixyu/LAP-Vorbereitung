Fahrplan App Beispiel Ausarbeitung:

beim ER Modell am Blatt habe ich das meiste richtig gemacht, aber ...
	° dem Kunden hätte ich einem Tarif gegeben, stattdessen lieber zur Fahrt speichern!
	° für jede Fahrt 2 FK für je eine Starthaltestelle und Endhaltestelle
	° bei der Fahrt werden 4 FK angegeben, für den Kunden, den Tarif und die Haltestellen
	° bei den Überlegungen, habe ich 2/3 NF vertauscht - 1NF = Werte Atmoar, 
							   - 2NF = Nicht-Schlüsselattribute hängen vom ganzen PK ab (zB Kundnedaten hängen nur vom idKunde ab)
							   - 3NF = keine Nicht-Schlüsselattribute voneinander abhängig (zB Tarifdaten sind in eigener Tabelle und nich bei jeder 								Fahrt neu definiert)
	° Das Modell ist leicht zu erstellen in MySQL - Beziehungen sind mit Foreign keys anscheinend automatisch eingetragen
	° DB-connection gleich erstellen und damit arbeiten
	° Forward Engineering ohne irgendwelche Kästchen anhaken
	° Script davon grundsätzlich korrekt, aber "VISIBLE" muss überall entfernt werden, sonst können Tables nicht erstellt werden!
	° auf Rechtschreibfehler achten!
		° Doppelklick auf Felder in der Schema Leiste, fügt Namen der Tabellen/Felder automatisch ein
	° INSERT issues: 1 Rechtschreibfehler und Duplicate entry '0' for key 'PRIMARY'
		° resolved: vergessen auto-increment anzugeben beim PK! bei allen PKs in allen Tabellen nachtragen
			° ALTER TABLE kunde MODIFY idKunde INT NOT NULL AUTO_INCREMENT;
	° bei DECIMAL keine € Zeichen, keine Leerzeichen als Tausendertrennung, keine "," sondern "." - alles hinter . = Dezimal
	° bei falsch eingefügten Daten - entfernen 
		° DELETE FROM kunde WHERE idKunde IN (1, 2, 3);
	° beim Löschen mit fix angegebenem Index merken - Index erhöht sich bei neuen Einträgen, nachdem 1,2,3 gelöscht wurde sind die nächsten 4, 5...
		° bei Prüfung theoretisch egal, kann jedoch mit:
			° DELETE FROM Tarif WHERE idTarif > 0;
			° ALTER TABLE tarif AUTO_INCREMENT = 1;
			° INSERTs
	° ich hätte jeweils 3 Einträge für Start und Endhaltestelle gemacht, das ist aber NICHT nötig! einfach 3 Haltestellen, für was diese benutzt werden ist benutzderabhängig!
	° bool wird as 0 oder 1 angegeben
